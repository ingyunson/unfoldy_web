# Unfoldy — Environment Variables
# These are SERVER-SIDE only (used by Vercel Serverless Functions).
# They are NOT exposed in the browser bundle.

# Google Gemini API key (for text + image generation)
GEMINI_API_KEY=your_gemini_key_here

# OpenAI API key (fallback for text + image generation)
OPENAI_API_KEY=your_openai_key_here
